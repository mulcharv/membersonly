extends layout 

block content 

    div(class='messageform-container')

        if storeduser 
            if storeduser.membership_status
                div(class='messageform-title') Create your message. 

                form(class='message_form' method='POST' action='')
                    div.form-group 
                        label(for='messagetext') Message Text: 
                        input#messagetext.form-control(class='messageinp' type='text', placeholder='Message Text' name='messagetext' required='true')
                    button.btn.btn-primary(type='submit') Submit

                if errors 
                    ul 
                        for error in errors 
                            li!= error.msg 
        
        else
            div(class='messageform-redirect') You need to be logged in and a member to create messages.